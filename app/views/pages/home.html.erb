<% if signed_in? %>
	<table class="front" summary="For signed-in users">
		<tr>
			<td class="main">
				<h1 class="micropost">What's up and stuff?</h1>
				<%= render 'shared/micropost_form' %>
				<%= render 'shared/feed' %>
			</td>
			<td class="sidebar round">
				<%= render 'shared/user_info' %>
				<%= render 'shared/stats' %>
			</td>
		</tr>
	</table>
<% else %>
	<h1>Twitter for Dummies</h1>
	<p>
	Who needs all those extra features? This is a simplified version of Twitter focusing on the core functionality.
	It was created thanks to Michael Hartl's wonderful <a href="http://railstutorial.org/">Ruby on Rails Tutorial</a>.
	Now sign up and start posting!
	</p>

	<%= link_to "Sign up now!", signup_path, :class => "signup_button round" %>
<% end %>